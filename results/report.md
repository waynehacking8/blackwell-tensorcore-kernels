# Blackwell vs Hopper Tensor Core GEMM — results

## NVIDIA RTX PRO 6000 Blackwell Max-Q Workstation Edition (sm_120)

![throughput](tflops_sm120.png)

At M=N=K=8192:

| kernel | TFLOP/s | % of FP32 cuBLAS | max abs err |
|---|---|---|---|
| naive | 4.7 | 8.6% | 0 |
| tiled | 7.2 | 13.2% | 0 |
| wmma | 39.7 | 72.6% † | 0.0112 |
| cublas | 54.7 | 100.0% | 0 |

> **% of FP32 cuBLAS** is vs `cublasSgemm` (FP32, CUDA cores). **†** This is a
> **precision-mismatched** comparison — the WMMA kernel uses FP16 Tensor Cores while
> `cublasSgemm` uses FP32 CUDA cores — and is **not** the Tensor Core ceiling. Rows
> above 100% (see `bench.csv`, e.g. 907.5% at size 512) reflect that FP16-TC vs
> FP32-CUDA-core mismatch (plus small-size launch overhead), not a kernel beating cuBLAS.
>
> A same-precision Tensor Core baseline, **`cublas_tc` (`cublasGemmEx`, FP16 in / FP32
> accumulate, `src/cublas_tc.cu`)**, has been added to the harness. After a re-run,
> `analysis/plot.py` regenerates this file with an additional **% of cuBLAS-TC** column —
> the honest same-precision ceiling. **Those numbers are pending a re-run on the sm_120
> box;** the WMMA `%` against `cublas_tc` is expected to be much lower than the FP32 figure
> above, because `cublasGemmEx` also runs on Tensor Cores.

> _Note: this file is auto-generated by `analysis/plot.py`. The table above is the last
> captured FP32-baseline run and is preserved verbatim; re-running `make analyze` after a
> fresh sweep will overwrite it with the new two-baseline format._

